# It is another kind of config file, and it is same as application.properties.
# The difference is that bootstrap.properties only be read by spring boot when it is under Spring Cloud
# framework. It is used for dynamic config, which means that you can change it frequently.
#server.port = 8080
